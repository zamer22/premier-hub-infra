apiVersion: v1
kind: Service
metadata:
  name: ml
  namespace: prod
spec:
  type: ClusterIP
  # IP fija — el backend la usa directo en ML_SERVICE_URL porque dnsPolicy:None no resuelve nombres del cluster
  clusterIP: 10.43.53.230
  selector:
    app: ml
  ports:
  - port: 8080
    targetPort: 8080
