apiVersion: apps/v1
kind: Deployment
metadata:
  name: pagina
  namespace: preprod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pagina
  template:
    metadata:
      labels:
        app: pagina
    spec:
      containers:
      - name: pagina
        image: premier-pagina:preprod
        imagePullPolicy: Never
        ports:
        - containerPort: 80
